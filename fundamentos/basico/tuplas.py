# Tuplas
# Secuencias inmutables
# Son similares a las listas pero tienen la diferencia que son inmutables
# Una vez creada, no pueden modificarse. tupla[1] = 11.0 -> TypeError
# Se definen usando el parentesis ()

coordenates = (10.5, 20.1)